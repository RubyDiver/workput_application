<div class="container">
  <div class="jumbotron">
    <h1>Workout Lounge!</h1>
    <h4>Show off your workout</h4>
  </div>
</div>

<div class="row">
  <div class="col-md-10">
    <h2>List of Members</h2>
    <%= will_paginate @athletes, renderer: BootstrapPagination::Rails, class: "pull-left paginateł" %>
  </div>
</div>
<div class="row">
  <% @athletes.each do |athlete| %>
    <div class="col-md-10 well member">
      <%= athlete.full_name %>
    </div>
  <% end %>
</div>